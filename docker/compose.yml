services:
  cvlab-kit:
    image: python:3.10-slim
    container_name: cvlab-kit
    ports:
      - "8000:8000"
    volumes:
      # Host paths (Docker auto-creates if missing)
      - /volume1/docker/cvlab-kit:/app
    working_dir: /app
    environment:
      - GIT_REPO=https://github.com/deveronica/cvlab-kit.git
    command: >
      bash -c "
      echo '🔧 Installing dependencies...';
      apt-get update -qq && apt-get install -y -qq git curl build-essential;
      if ! command -v node > /dev/null; then
        echo '📦 Installing Node.js...';
        curl -fsSL https://deb.nodesource.com/setup_18.x | bash -;
        apt-get install -y -qq nodejs;
      fi;
      pip install uv;

      if [ ! -d .git ]; then
        echo '📥 Initial setup: Cloning repository...';

        # Clone to temp dir, then copy to /app
        git clone $$GIT_REPO /tmp/cvlab-clone || { echo '❌ Git clone failed!'; exit 1; };
        cp -a /tmp/cvlab-clone/. /app/ || { echo '❌ File copy failed!'; exit 1; };
        rm -rf /tmp/cvlab-clone;

        mkdir -p logs outputs config web_helper/state web_helper/queue_logs;
        uv sync --frozen || { echo '❌ uv sync failed!'; exit 1; };
        cd web_helper/frontend && npm ci && npm run build && cd ../.. || { echo '❌ Frontend build failed!'; exit 1; };
        echo '✅ Setup complete!';
      else
        echo '🔄 Updating from git...';
        mkdir -p logs outputs config web_helper/state web_helper/queue_logs;
        git pull;
        uv sync --frozen;
        if git diff --name-only HEAD@{1} HEAD 2>/dev/null | grep -q 'web_helper/frontend/src'; then
          echo '🎨 Frontend changed, rebuilding...';
          cd web_helper/frontend && npm run build && cd ../..;
        fi;
      fi;

      echo '🚀 Starting application...';
      exec uv run app.py
      "
    restart: unless-stopped
